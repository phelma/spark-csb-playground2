import { createTheme, uswitchTheme as uswitchSparkTheme } from "@uswitch/spark";
import uswitchTrustyleTheme from "@uswitch/trustyle.uswitch-theme";

const theme = createTheme(uswitchTrustyleTheme, uswitchSparkTheme);

export default theme;
